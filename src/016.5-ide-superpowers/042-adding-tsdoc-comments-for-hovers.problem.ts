/**
 * Adds two numbers together
 *
 * @param a number
 * @param b number
 * @returns number
 *
 * @example
 * myFunction(23, 34);
 */
const myFunction = (a: number, b: number) => {
  return a + b;
};
